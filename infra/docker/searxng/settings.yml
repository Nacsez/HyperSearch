use_default_settings: true

server:
  secret_key: "replace-me-in-production"
  limiter: false
  image_proxy: false

search:
  safe_search: 1
  autocomplete: ""
  formats:
    - html
    - json

ui:
  static_use_hash: true

redis:
  url: false

